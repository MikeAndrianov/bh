<!--<div class="row">
	<div class="col-md-offset-1 col-md-10 cont text-center">
		<h1 class="max">Bottle Hunter</h1>
		<div>
		  {{outlet}}
		</div>
		{{#if signedIn}}
		  {{#link-to 'sign_out' class='btn btn-danger'}}Sign Out{{/link-to}}
		{{else}}
		  {{#link-to 'sign_in' class='btn btn-success'}}Sign In{{/link-to}}
		{{/if}}
	</div>

</div>
-->
 <!-- Header -->
  
  <!--  <header id="top" class="header">
        <div class="text-vertical-center">
        	<div class="row">
        		<div class="col-md-6 col-md-offset-3">
	        		<h1>Bottle Hunter</h1>
	            	<h3>Welcom Bottle Hunter project!</h3>
	        	</div>
        	</div>
        	<div class="row">
        		<div class="col-md-4 col-md-offset-4">
	        		<a href="#about" class="btn btn-default">Find Out More</a>
	        	</div>
        	</div>
        	<br>
        	<div class="row">
        		<div class="col-md-4 col-md-offset-4">
		        	{{#if signedIn}}
					  {{#link-to 'sign_out' class='btn btn-danger'}}Sign Out{{/link-to}}
					{{else}}
					  {{#link-to 'sign_in' class='btn btn-success'}}Sign In{{/link-to}}
					{{/if}}
	        	</div>
        	</div>
        	<div class="row">
        		<div class="col-md-6 col-md-offset-3">
				  {{outlet}}
				</div>
        	</div>
        </div>
    </header>
  -->

  <div class="outer">

        <div class="middle row">
	        <div class="col-sm-3 col-md-4"></div>
	        <div class="inner opacity col-xs-12 col-sm-6 col-md-4">
	            <div class="row">
	                <h1>Bottle Hunter</h1>
	                <h4>Welcom Bottle Hunter project!</h4>
	            </div>
	            <br>
	            <div class="row">
	            	<div class="col-xs-12 col-sm-6 col-md-6 marg">
	            		<a href="#about" class="btn btn-default">Find Out More</a>
	            	</div>
	            	<div class="col-xs-12 col-sm-6 col-md-6 marg">
	            		{{#if signedIn}}
						  {{#link-to 'sign_out' class='btn btn-danger'}}Sign Out{{/link-to}}
						{{else}}
						  {{#link-to 'sign_in' class='btn btn-success'}}Sign In{{/link-to}}
						{{/if}}
	            	</div>
	            </div>
	            <div class="row">
					{{outlet}}
	        	</div>
	        </div>
	        <div class="col-sm-3 col-md-4"></div>
        </div>

    </div>