// This is the same as default route for company and therefore shouldn't be written explicitly.
// Placed here just for example

// Bh.CompanyRoute = Ember.Route.extend({
//   model: function(params) {
//     return this.store.find('company', params.company_id);
//   }
// });
